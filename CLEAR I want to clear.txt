- event-propagation
- bubbling
- stop propagation 
- prevenDefault
- array method- instance method vs static method
- difference: "let array-variable" VS only "array-variable
- const numSlice7 = nums.slice(1, 6); // [ 1, 2, 3, 4, 5 ] ?????? is it common to use???
    console.log("nums.slice(1,6) =", numSlice7); 
    let nums = [0, 1, 2, 3, 4, 5];
- does sort() sorts numbers?
- methods that "modifies" the "original Array" ???
- fill() details
- vite
- dev vite
- import export types common js , moder js
- build: vite build
- dev dependecies vs dependency
- package-lock.json 
- dist
- bin
-  npm run dev ?
- hot reload ?
- tailwind utilities & others apply to id ???
- is transition utility in tailwind?
- utilities have multiple css properties?
- const nums2 = [11, 45, 33, 99, 100, 91]
    const numSortCallBkFn = nums2.sort((a,b)=> a-b);
    console.log("numSortCallBkFn", numSortCallBkFn);

    here ((a,b)=> a-b); does what??

- array methid docs

- const nestedArrayReduce = [1, 6, 7, 9, 3, [2, 24]];

const reduce = nestedArrayReduce.reduce((prevValue, cuurentValue, index, array)=> prevValue + cuurentValue)

console.log("reduce", reduce); // 262,24 
??? how = 262,24

- list of method chaining
- event capture
    is it default? 
    why true in parent in example 2
    why need to know?
    effect?
- event handler
- synchronus vs asynchronous
- template
- traverse
 - content painting ??
 - - content painting: to avoid, template is used in DOM
    when update occurs in js, js startsover from 1st, rather than,
    updatimg/patching the selected part.
    to avoid it template is used ???

- range ?
    - window.getSelection() ??- 

- Multiple Promises ? before promise all
- resolve vs settled?? 
- "then" keyword
- virtualization
- pagination
- fetch
- curl
- micro service architecture" comes from this concept, to scale up
- latency
- syntatic-sugar list
- prototype chaining
- why no "this-keyword" before the method name inside class??
- Object Composition
    eg. vs code extension why?