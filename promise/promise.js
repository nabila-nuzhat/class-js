// promise ref source: https://dev.to/shameel/understanding-promise-in-javascript-45kc
    // settle status 2 types 
       // resolve/ reject
       // behind the scene 4 step: but main step 3
        // pending, resolve, reject
        // pending, resolve, reject/settle (behind the scene)
        // Difference between Promise.all() and Promise.allSettled()